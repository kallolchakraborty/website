{
  "name": "kallol-website-github-pages",
  "private": true,
  "scripts": {
    "build": "python3 build.py && tailwindcss -i ./static/input.css -o ./static/site.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
